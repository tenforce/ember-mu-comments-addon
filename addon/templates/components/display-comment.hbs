<div class="top">
    <p class="author">{{comment.authorLabel}}</p>
    <div class="buttons">
        <button type="button" name="deleteComment" disabled={{not comment.isModifiable}} {{action "deleteComment" comment}}> X </button>
        <button type="button" name="modifyComment" disabled={{not comment.isModifiable}} {{action "changeModifyState" }}> {{modValue}}  </button>
    </div>
</div>

<div class="middle">
    <input type="checkbox" checked={{isActive}} {{action "changeStatus" on="change"}}/>
    <p class="content">
        <textarea onkeyup={{action "textContentModified"}} class="displayText" disabled={{isNotModify}}>{{textContent}}</textarea>
        {{!input type="textarea" class="displayText" value=textContent disabled=isNotModify }}
    </p>
</div>
<div class="bottom">
    <p class="date">{{format-relative comment.date type="numeric" interval=10000}}</p>
</div>
